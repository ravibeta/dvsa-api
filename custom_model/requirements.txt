# Dependencies for the custom ONNX model adapter (custom_model/).
#
# These are intentionally kept minimal and decoupled from the main dvsa-api
# requirements so the adapter can be vendored / installed independently.
#
#   pip install -r custom_model/requirements.txt
#
# Notes
# -----
# * onnxruntime provides the inference session. Use onnxruntime-gpu instead if
#   you have a CUDA-capable host and the matching CUDA/cuDNN runtime.
# * The unit tests mock onnxruntime.InferenceSession, so onnxruntime is NOT
#   required merely to run `pytest`. It IS required to run real inference.

onnxruntime>=1.16,<2.0
numpy>=1.23
opencv-python>=4.7
requests>=2.28
pytest>=7.0
