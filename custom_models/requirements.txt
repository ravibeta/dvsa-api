# Dependencies for the multi-format model layer (custom_models/).
#
#   pip install -r custom_models/requirements.txt
#
# Only numpy + opencv-python are needed to run the unit tests, which mock the
# runtimes (onnxruntime / torch / ultralytics) via injected sessions/factories.
# The runtime packages below are required only for *real* inference and are
# commented by format so you can install just what you use.

numpy>=1.23
opencv-python>=4.7
requests>=2.28
pytest>=7.0

# --- ONNX format (onnx adapter) -------------------------------------------
onnxruntime>=1.16,<2.0
# onnxruntime-gpu  # use instead of onnxruntime on a CUDA host

# --- PyTorch / TorchScript format (torch adapter) -------------------------
# torch>=2.0
# torchvision>=0.15

# --- Ultralytics YOLO format (yolo adapter) -------------------------------
# ultralytics>=8.0   # pulls in torch
